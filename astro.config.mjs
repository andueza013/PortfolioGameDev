import { defineConfig } from 'astro/config';

export default defineConfig({
  // Reemplaza con tu usuario y el nombre exacto de tu repositorio
  site: 'https://andueza013.github.io',
  base: '/PortfolioGameDev',
});
